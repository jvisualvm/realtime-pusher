spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:client/resource/db/manager.db


logging:
  level:
    root: info

thread:
  pool:
    corePoolSize: 9
    maximumPoolSize: 64




mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl